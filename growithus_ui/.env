# Vite + React + TypeScript + Tailwind CSS Template

This template provides a starting point for building modern web applications using Vite, React, TypeScript, and Tailwind CSS.

## Features

- Vite for fast development and build
- React with TypeScript
- Tailwind CSS for styling
- Hot Module Replacement (HMR)
- Production-ready build

## Getting Started

### Prerequisites

- Node.js (version 14 or higher)
- npm or yarn

### Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>
   ```

2. Navigate to the project directory:
   ```bash
   cd <project-name>
   ```

3. Install dependencies:
   ```bash
   npm install
   ```

### Development

To start the development server:

```bash
npm run dev
```

This will start the development server at `http://localhost:5173`.

### Building for Production

To build the application for production:

```bash
npm run build
```

The built files will be located in the `dist` directory.

### Environment Variables

Create a `.env` file in the root directory to manage environment-specific variables:

```env
VITE_API_BASE_URL=http://localhost:3001/api
```

Make sure to update the `VITE_API_BASE_URL` value according to your deployment environment.

## Project Structure

```
src/
├── assets/          # Static assets
├── components/      # Reusable components
├── hooks/           # Custom hooks
├── lib/             # Utility functions
├── pages/           # Page components
├── services/        # API service files
├── store/           # State management (if using Redux/Zustand)
├── styles/          # Global styles
├── types/           # TypeScript type definitions
├── utils/           # Utility functions
├── App.tsx          # Main App component
├── main.tsx         # Entry point
└── vite-env.d.ts    # TypeScript declarations
```

## Deployment

When deploying to production, ensure that:

1. The `VITE_API_BASE_URL` environment variable is set correctly for your deployment environment.
2. The build output is served from the correct path.
3. Any necessary proxy configurations are in place if using a reverse proxy.

## Contributing

Feel free to contribute by submitting issues or pull requests.

## License

This project is licensed under the MIT License.
